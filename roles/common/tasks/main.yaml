---
 - name: Install common packages
   apt: name={{item}} state=present update_cache=no
   with_items:
    - htop
    - psmisc
    - mc
    - wget
    - curl
    - vim
    - iftop
    - tcpdump
    - gdebi
    - software-properties-common
    - tree
